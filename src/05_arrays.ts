var arrayOfNumbers: number[] = [1, 2, 3];
console.log(arrayOfNumbers);

//arrayOfNumbers = ["one", "two", "three"]; // gives error as the original had integer in it.

//let arrayOfNumberAndStrings = [1, "2", 3]; //gives error, doesn't accept mixed values

arrayOfNumbers.push(5);
console.log(arrayOfNumbers); //old js push function.

let arr: number [] = []; //defining an array that takes number

arr[0] = 3;
console.log(arr);

//arr[2] = "4"; //not allowed.